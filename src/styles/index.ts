import { layout_styles } from './_layout'
import { global_styles } from './_global'

/**
 * Add and import your own custom styles here
 */

export const styles = {
  ...global_styles,
  ...layout_styles,
}
